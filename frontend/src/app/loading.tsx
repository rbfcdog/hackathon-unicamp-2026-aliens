export default function Loading() {
  return (
    <main className="boot-screen" aria-label="Carregando workspace">
      <div className="brand-glyph" aria-hidden="true"><i /><i /><i /></div>
      <span>Inicializando workspace</span>
    </main>
  );
}
